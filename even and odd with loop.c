#include<stdio.h>
int main()
{
 int i, num, odd, even;
 printf("Enter your value:\n ",num);
 scanf("%d",&num);
  for (i = 1; i <= num; i++)
    {
        if (i % 2 == 0)
            even;// = even+ i;
        else
            odd; //= odd + i;
    }
    printf("Sum of even numbers  = %d\n", even);
    printf("Sum of odd numbers = %d\n", odd);

}
