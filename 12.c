#include <stdio.h>

int main()
{
    int a, b, i, j, flag, temp, count = 0;

    printf("Enter the value of num1 and num2 \n");
    scanf("%d %d", &a, &b);
    if (b < 2)
    {
        printf("There are no primes upto %d\n", b);
        exit(0);
    }

}
